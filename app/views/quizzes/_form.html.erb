<%= form_for @quiz do |quiz| %>
  <% if @quiz.errors.any? %>
      <div class="error_messages">
        <h2><%= pluralize(@quiz.errors.count, "error") %> prohibited this survey from being saved:</h2>
        <ul>
          <% @quiz.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
  <% end %>

  <%= quiz.label 'Quiz Title' %>
  <%= quiz.text_field :title %>

  <%= quiz.fields_for :questions do |builder| %>
    <%= render 'question_fields', f: builder %>
  <% end %>

  <%= quiz.submit "Submit" %>
<% end %>